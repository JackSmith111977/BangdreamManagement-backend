<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kei.tliaswebmanagement1.mapper.StudentMapper">

    <!-- 根据条件查询学生信息 -->
    <select id="select">
        select id, name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, violation_count, violation_score, create_time, update_time
        from student
        <where>
            <if test="name!=null and name!=''">
                name like concat('%',#{name},'%')
            </if>
            <if test="degree!=null">
                and degree=#{degree}
            </if>
            <if test="clazzId!=null">
                and clazz_id=#{clazzId}
            </if>
        </where>
    </select>

    <!-- 添加学生信息 -->
    <insert id="insert">
        insert into tlias.student
            (name,no,gender,phone,
            <if test="idCard!=null and idCard!=''">
                id_card,
            </if>
            <if test="isCollege!=null">
                is_college,
            </if>
            <if test="address!=null and address!=''">
                address,
            </if>
            <if test="graduationDate!=null">
                graduation_date,
            </if>
            degree,clazz_id,create_time,update_time)
        values (#{name},#{no},#{gender},#{phone},
        <if test="idCard!=null and idCard!=''">
            #{idCard},
        </if>
        <if test="isCollege!=null">
            #{isCollege},
        </if>
        <if test="address!=null and address!=''">
            #{address},
        </if>
        <if test="graduationDate!=null">
            #{graduationDate},
        </if>
        #{degree},#{clazzId},#{createTime},#{updateTime}  )
    </insert>

    <!-- 根据id查询学生信息 -->
    <update id="update">
        update tlias.student
        <set>
            <if test="name!=null and name!=''">
                name=#{name},
            </if>
            <if test="no!=null and no!=''">
                no=#{no},
            </if>
            <if test="phone!=null and phone!=''">
                phone=#{phone},
            </if>
            <if test="gender!=null">
                gender=#{gender},
            </if>
            <if test="degree!=null">
                degree=#{degree},
            </if>
            <if test="idCard!=null">
                id_card=#{idCard},
            </if>
            <if test="isCollege!=null">
                is_college=#{isCollege},
            </if>
            <if test="address!=null and address!=''">
                address=#{address},
            </if>
            <if test="graduationDate!=null">
                graduation_date=#{graduationDate},
            </if>
            <if test="clazzId!=null">
                clazz_id=#{clazzId},
            </if>
            <if test="violationCount!=null">
                violation_count=#{violationCount},
            </if>
            <if test="violationScore!=null">
                violation_score=#{violationScore},
            </if>
        </set>
        where id=#{id}
    </update>

    <!-- 根据id删除学生信息 -->
    <update id="handleViolation">
        update tlias.student
        <set>
            violation_count=violation_count+1,
            violation_score=violation_score+#{score},
            update_time=#{now}
        </set>
    </update>

    <!-- 统计班级人数-->
    <select id="getClazzStudentCount" resultType="java.util.Map">
        select class.name class,count(student.id) data from tlias.student left join tlias.class on student.clazz_id = class.id
        group by class.name;
    </select>

    <!-- 统计学生学历分布-->
    <select id="getStudentDegreeData" resultType="java.util.Map">
        select
            case when degree=1 then '初中'
            when degree=2 then '高中'
            when degree=3 then '大专'
            when degree=4 then '本科'
            when degree=5 then '硕士'
            when degree=6 then '博士'
            end as name,
            count(id) as value
            from tlias.student group by degree;
    </select>
</mapper>
