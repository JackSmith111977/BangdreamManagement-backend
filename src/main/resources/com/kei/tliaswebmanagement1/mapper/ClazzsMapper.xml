<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kei.tliaswebmanagement1.mapper.ClazzsMapper">

    <select id="selectByClazzsQueryParam" resultType="com.kei.tliaswebmanagement1.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, subject, create_time, update_time from class
            <where>
                <if test="name!=null and name!=''">
                    name=#{name}
                </if>
                <if test="begin!=null and end!=null">
                    and end_date between #{begin} and #{end}
                </if>
            </where>
    </select>

    <!--添加班级信息-->
    <insert id="insert">
        insert into class (name, room, begin_date, end_date, master_id, subject, create_time, update_time)
        values (#{name},#{room},#{beginDate},#{endDate},#{masterId},#{subject},#{createTime},#{updateTime});
    </insert>

    <!--修改班级信息-->
    <update id="update">
        update tlias.class set name=#{name},room=#{room},begin_date=#{beginDate},end_date=#{endDate},
                               <if test="subject!=null">subject=#{subject},</if>
                               master_id=#{masterId}
        where id=#{id};
    </update>



</mapper>
