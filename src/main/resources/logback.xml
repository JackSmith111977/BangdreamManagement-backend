<?xml version="1.0" encoding="utf-8" ?>
<configuration>

    <!-- 控制台输出 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d 表示日期，%thread 表示线程名，
            %-5level表示级别从左显示5个字符宽度，-5是占五个字符宽度的意思，
            %logger是日志记录器的名称，花括号数字表示名称最大字符数，%msg表示日志消息，%n表示换行符 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50}-%msg%n</pattern>
        </encoder>
    </appender>

    <!-- 按照每天生成日志文件 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 路径-%d{日期}-%i.log,
            日志文件输出的文件名, %i表示序号 -->
            <!--windows环境的路径-->
            <!--<FileNamePattern>log/log-%d{yyyy-MM-dd}-%i.log</FileNamePattern>-->
            <!--linux环境路径-->
            <FileNamePattern>log/log-%d{yyyy-MM-dd}-%i.log</FileNamePattern>
            <!-- 最多保留的历史日志文件数量 -->
            <MaxHistory>30</MaxHistory>
            <!-- 最大文件大小，超过这个大小会触发滚动到新文件，默认为 10MB -->
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>

        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d 表示日期，%thread 表示线程名，%-5level表示级别从左显示5个字符宽度，%msg表示日志消息，%n表示换行符 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50}-%msg%n</pattern>
        </encoder>
    </appender>

    <!-- 日志输出级别，ref即引用前两个配置的name属性 -->
    <!-- 日志级别有以下类型：(级别从低到高)
         trace 追踪     log.trace("...")
         debug 调试     log.debug("...")
         info  一般信息  log.info("...")
         warn  警告信息  log.warn("...")
         error 错误信息  log.error("...")-->
    <!-- 只有大于等于level属性等级的日志才会输出-->
    <root level="INFO">
        <!--输出到控制台-->
        <appender-ref ref="STDOUT" />
        <!--输出到文件-->
        <appender-ref ref="FILE" />
    </root>

</configuration>